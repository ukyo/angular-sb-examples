import { HighlightjsDirective } from './highlightjs.directive';

describe('HighlightjsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightjsDirective();
    expect(directive).toBeTruthy();
  });
});
